import { JSXNode } from "jsx-xml";

type children = JSXNode;
type name = string;
type expr = string;
type cond = string;
type type = string;
type slot = string;
type modal = string;

export { children, name, expr, cond, type, slot, modal };
